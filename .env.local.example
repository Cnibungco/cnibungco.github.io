# Portfolio Basic Auth Credentials
# Generate strong password: openssl rand -base64 32
PORTFOLIO_USERNAME=camille
PORTFOLIO_PASSWORD=saintmotel

# Optional: Set to 'development' to disable auth locally
NODE_ENV=production
